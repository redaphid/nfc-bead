# Iter 07 — anim cycle test (PARTIAL — superseded by anim_*.py refactor)

The user invoked `auto_cycle.py` to rotate through the 4 looping animations every 30 s; this folder captured one render along the way (`02_locked_profile.png` — the locked side profile shot, pre-color-bump palette).

After this iter the work pivoted to:
- High-contrast palette (ultramarine / emerald / vermilion) committed as the canonical EEVEE values in `architect_on.py`
- Animation cycling via `auto_cycle.py` registered as a `bpy.app.timers` ticker (no notes file per cycle)
- Then the `tools/` host-side STL/3MF tooling on uv

Kept the screenshot as a reference of the pre-bump palette under the warm key sun for future "did it really look better after the bump" comparisons. Not a full iter — just a visual breadcrumb.
