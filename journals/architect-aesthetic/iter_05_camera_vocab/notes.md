# Iter 05 — camera vocabulary (PRE-PIVOT — alternative explored)

**Status: superseded by the floating direction.** These renders were captured before the user redirected to "eerily suspended, perfect forms" (see `../iter_05_floating/PIVOT.md`).

The iter 04 scene (with graph-paper plate) was used to test 5 camera shots. They show the bead RESTING on the plate, which the user later rejected. Kept here as a visual record of the rejected direction — useful reference if a future session ever wants to revisit "bead on graph paper."

## Shots rendered

- `render_A_wide_establishing.png` — canonical wide, cam (0,-50,18) lens 50, target (0,0,1.5)
- `render_B_locked_profile.png` — eye-level horizontal silhouette, cam (0,-50,2.5) lens 50, target (0,0,2.5)
- `render_C_top_down.png` — pure overhead, cam (0,0,45) lens 50
- `render_D_macro_spiral.png` — close on the spiral, cam (18,-15,5) lens 80
- `render_E_macro_bottom.png` — close on the bottom-half NFC pocket area, cam (-18,-15,5) lens 80

## Why these are kept on the branch

Each shot's CAMERA PARAMETERS are still the right framings — they got carried into the new `anim_locked_profile.py`, `anim_top_down.py`, `anim_macro_pull.py` scripts. Only the GROUND was wrong, not the angles. So these renders document "framings that worked, on a scene composition that didn't."
